services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: hiremind
      POSTGRES_PASSWORD: hiremind_dev
      POSTGRES_DB: hiremind
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: